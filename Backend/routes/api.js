// This file contains additional API routes that can be expanded later
const express = require('express');
const router = express.Router();

// You can add more specific routes here as your app grows
// For example, authentication routes, product management, etc.

module.exports = router;